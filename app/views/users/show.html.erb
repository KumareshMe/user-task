<h1 class="text-center mt-4"><%= @user.username %> profile </h1>

<h2 class="text-center mt-4">Assigned Tasks</h2>

<%= render 'tasks/task' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @tasks, :container => false %>
</div>